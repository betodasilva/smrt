import { ContentOffsetDirective } from './content-offset.directive';

describe('ContentOffsetDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentOffsetDirective();
    expect(directive).toBeTruthy();
  });
});
